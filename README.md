# rabi
Stable conversion of structs between two separate Rust programs.

## Derive
`rabi = { ..., features = ["derive"] }`

## Examples
You can find examples of use in [rabi_tests](rabi_test/tests/).
